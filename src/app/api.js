// https://www.jsonplaceholder.org/

export const baseUrl = "https://jsonplaceholder.org";

const posts_api = {
  posts: "/posts",
};

const comments_api = {
  comments: "/comments",
};

const users_api = {
  users: "/users",
};

export { posts_api, comments_api, users_api };
